import { PlayerCard } from '@/app/(index)/(dynamic-routes)/player/uid/[uid]/_components/player-card'

export default async function PlayerUidPage() {
  return (
    <section className='w-full'>
      <PlayerCard />
    </section>
  )
}
